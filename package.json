{
  "name": "doughlicious",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "node scripts/start-dev.js",
    "dev:backend": "pnpm --filter doughlicious-backend dev || npm run dev --prefix backend",
    "dev:frontend": "pnpm run sync-env && (pnpm --filter doughlicious-frontend dev || npm run dev --prefix frontend)",
    "sync-env": "node scripts/sync-env.js"
  },
  "devDependencies": {
    "concurrently": "^9.1.0"
  }
}

